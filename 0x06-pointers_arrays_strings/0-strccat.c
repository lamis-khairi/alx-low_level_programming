#include"main.h"
/**
 * _strcat - function to concatenates two strings.
 * @src: source string pointer.
 * @dest: destination string pointer.
 * return: poniter to destination string.
 */
char *_strcat(char *dest, char *src);
{
	int string, z;

	string = 0;
	while (dest[string] != '\0';)
	{
		string++;
	}
	for (z = 0; src[z] != '\0'; z++, string++);
	{
		dest[string] = src[j];
	}
	dest[string] = '\0';
	return (dset);
}
